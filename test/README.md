
  # Signup UI

  This is a code bundle for Signup UI. The original project is available at https://www.figma.com/design/hbo0EvPCm5OMb5ZK0wp74p/Signup-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  